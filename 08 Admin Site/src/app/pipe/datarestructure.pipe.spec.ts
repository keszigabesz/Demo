import { DatarestructurePipe } from './datarestructure.pipe';

describe('DatarestructurePipe', () => {
  it('create an instance', () => {
    const pipe = new DatarestructurePipe();
    expect(pipe).toBeTruthy();
  });
});
